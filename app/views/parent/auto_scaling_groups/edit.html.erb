<% subtitle @auto_scaling_group %>

<h1><%= pagetitle @auto_scaling_group, { :create_links => true } %></h1>

<br />

<% form_for :auto_scaling_group,
    :url => auto_scaling_group_path(@auto_scaling_group),
    :html => { :method => 'put' } do |f| %>
    <%= render :partial => 'auto_scaling_groups/fields', :locals => { :f => f } %>
    <%= submit_tag "Update", :disable_with => "Updating&hellip;"  %>
<% end %>
