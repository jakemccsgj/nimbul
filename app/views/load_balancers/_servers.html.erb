<%= collection_select(nil, :server_id, @servers, :id, :name,
  {:prompt   => "select a server"},
  {
    :onchange => "#{remote_function(
      :url  => {:action => "update_instances"},
      :with => "'cluster_id=#{params[:cluster_id]}&server_id='+value")};"
  }
)
-%>
