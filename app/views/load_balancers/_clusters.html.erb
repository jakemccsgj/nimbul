<%= collection_select(nil, :cluster_id, @clusters, :id, :name,
  {:prompt   => "select a cluster"},
  {:onchange => "#{remote_function(
    :url  => {:action => "update_servers"},
    :with => "'cluster_id='+value")};"
  }
)
-%>

